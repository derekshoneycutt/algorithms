# shlib

This directory is primarily for shell "library" files sourced by `run.sh` and `init.sh`.

Most files here define shared helper functions/modules.

`run-smoke-test.sh` is different: it is a standalone development utility script used for smoke testing.
