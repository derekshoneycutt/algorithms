# Get the maximum value of a set of numbers


#Q'Nial really doesn't have command line arguments so we just use default values

A := [23, 43, 86, 34, 20, 58];
write (['values: '] link A)
write ('max: ' link string (max A))

bye
